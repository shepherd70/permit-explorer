# permit-explorer — task tracker

Status values: `todo` | `in-progress` | `done` | `dropped`

| ID | Task | Status | Notes |
|----|------|--------|-------|
| 1 | Profile raw permits CSV (schema, nulls, distributions) | done | 1,498 rows, 1999–2026; 74 rows missing cost; no row has both cost and sqft |
| 2 | Build interactive dashboard (KPIs, charts, filters, map, table) | done | Single-file HTML; Chart.js + Leaflet via CDN; data embedded |
| 3 | Add deep-dive analytics (buildout timeline, cost/DTI distributions, renovation lifecycle) | done | Renovation lifecycle pairs new-build → first renovation per address (107 pairs, median ~3.2 yrs) |
| 4 | Headless verification harness (Node, DOM stubs) | done | KPIs cross-checked against pandas; committed under test/ (see #6) |
| 5 | Set up repo (local C:\dev\permit-explorer + GitHub remote) | done | Private repo github.com/shepherd70/permit-explorer; main + session branch pushed |
| 6 | Commit verification harness as `test/harness.js` | done | test/harness.js + test/city_harness.js, self-extracting from the HTML; run with node |
| 7 | City-wide live API + community drill-down | done | src/city_explorer.html: live API explorer, all 490K+ permits, community/type/status filters |
| 8 | $/sqft analysis | dropped | No permit in current export has both cost and TotalSqFt |
| 9 | Harden city explorer: request-race guard | done | AbortController + per-request generation token in apply()/enterCity/enterDetail/cityTable so rapid filter/sort changes can't render stale data |
| 10 | Shareable URL filters + CSV export | done | Filters encoded in URL query string (read on load, written on apply); Export CSV button (client-side in detail mode, Socrata `.csv` endpoint capped at 50K in city mode) |
| 11 | Resilience & a11y polish | done | CDN-failure guard, 429 rate-limit message, empty-state pagination text, map region label, paginator aria-labels |
| 12 | Real test assertions + CI | done | Harnesses now assert (exit non-zero on failure) incl. a race-condition regression test; package.json `npm test`; GitHub Actions CI |
| 13 | Ship infra: LICENSE + Pages deploy | done | MIT LICENSE w/ Calgary data attribution; .github/workflows/pages.yml publishes explorer as site root |
| 14 | Launch: enable hosting for the city-wide explorer | done | 2026-06-17: repo made public, GitHub Pages enabled (Actions source), deploy workflow run. **Live at https://shepherd70.github.io/permit-explorer/** — explorer as site root (index.html) + offline dashboard. |
| 15 | Smoke test against the live Socrata API | done | `test/smoke.js` + `npm run smoke`: hits the real c2es-76ed endpoint and verifies all 16 fields are selectable (per-field diagnosis on 400), row count is plausible, and the SoQL features the app uses still work (`date_extract_y`, `sum`/`avg`/`date_diff_d`, `case()` bins, CSV export header). Self-extracts the field list + endpoint from `src/city_explorer.html` so it can't drift; retries transient 429/5xx/network but hard-fails on schema breaks. Scheduled daily via `.github/workflows/smoke.yml` (+ manual dispatch); not part of `npm test` (kept offline/deterministic). |
| 16 | Offline dashboard data freshness | done | Footer + subtitle are now build-time derived (was a hardcoded "…20260612.csv … generated 2026-06-12" / "1999–2026", which would silently lie after any rebuild with new data). `build.py` computes the coverage year range, permit count, and a "data as of" export date (parsed from the `YYYYMMDD` filename, falling back to file mtime) and fills `__COVERAGE__`/`__FOOTER__` placeholders; the footer also states the live explorer is canonical/always-current, and the README frames the offline file as a point-in-time demo. Placeholders sit outside the `<script>`, so the harness eval contract is untouched; harness still 16/16. |
| 17 | Community map: choropleth option (bubbles → gradient polygons) | done | **Shipped 2026-06-20** as a bubbles↔areas toggle (city mode) with a metric selector — avg project cost · avg days-to-issue · completion rate — all from the existing community query (extended with `avg(date_diff_d)` + `case()` sums; no extra requests). Live lazy-fetched `surr-xmvs` boundaries (cached); direct `UPPER(name)`↔`communityname` join covers ~99.4% of permit volume (270/317; residual/N-A/CFB/new areas → no-data, no alias map needed); cividis ramp, 2nd–98th-pct clamp (log for cost), min-n gate, continuous gradient legend. All `L.geoJSON` calls data-gated/guarded so the single-`<script>` harness contract holds; city harness extended (L.geoJSON stub + canned boundaries + 5 assertions, 34/34); src/dist synced. Follow-up (done in #18): view state now URL-encoded. **Original evaluation 2026-06-14 (planning):** Recommendation: do NOT replace the count bubbles (bubble area encodes count honestly regardless of polygon size); instead add a choropleth as a *toggle* for an intensive metric (avg project cost / avg days-to-issue / completion rate) that needs no area denominator — or, to map "how much building," normalize to permits-per-1,000-dwellings (needs Civic Census dwelling counts). Stay in Leaflet (`L.geoJSON`), not R/ggplot2 (would regress the interactive tool to a static image). Source = Calgary "Community District Boundaries" GeoJSON (has `name` + `comm_code`). Key risks: name-join (permits API exposes `communityname` only → needs normalized key + alias map for mismatches); CRS (display EPSG:4326, compute any area in EPSG:3776); classification jitter under live filters (prefer continuous sequential, clamp 2nd–98th pct, log for skew); colourblind/grayscale-safe sequential ramp (cividis/viridis/Blues); distinct no-data + min-n gate. Must preserve the single-`<script>` harness contract, guard browser-only Leaflet APIs, and keep src/ and dist/ in sync. |
| 18 | Shareable URL state for the map view (bubbles/areas + metric) | done | `readURL`/`writeURL` now also encode the map view: `map=areas` when the choropleth is on (omitted for the default bubbles) and `metric=<dti\|comp>` for a non-default metric (default `cost` omitted to keep links clean). A shared `?map=areas` link self-heals on first paint — `updateMap` lazy-loads boundaries via a new idempotent `ensureBoundaries()` and redraws — which let `setChoro`/`setChoroMetric` collapse to set-state + `writeURL` + `updateMap`. City harness extended with a `location`/`history` stub and 6 round-trip assertions (40/40); src/dist synced. |
| 19 | Repo audit (2026-06) — findings-first | done | Branch audit/permit-explorer-2026-06; report docs/permit-explorer-audit-2026-06.md (PR #10). 0 critical, 2 major, 17 minor; pi_audit clean, git provenance clean. |
| 20 | Fix audit majors + add drift guard | done | PR session/2026-06-21-audit-fixes. Offline XSS fixed (template.html `esc()` on table/popup/insights); build.py cost parsing made dtype-robust; drifted `fmt$`/`fmtN` synced to the live copy; new `test/drift.js` asserts shared helpers stay identical across the two files (wired into `npm test`). dist rebuilt; 16+40+9 tests green. |
| 25 | Real community dropdown + instant client-side search | done | PR session/2026-06-21-index-root. (1) Community filter is now a real `<select>` (317 communities A→Z + an "All communities" reset option), replacing the free-text `<input list=datalist>` that didn't behave like a pull-down and needed an exact typed match. (2) Search no longer runs Socrata's `$q` full-text aggregate (`count(1)+$q` measured ~4s): a new `D.onSearch()` filters the already-loaded **detail** rows **client-side** (instant, zero requests; count badge = filtered total), and in **city** view (no rows loaded) shows a "narrow first" hint (`#search-hint`) instead of the slow query. City harness 65→72; live-verified on Harvest Hills (search "160" → 32 rows, 0 server requests); `dist` rebuilt/synced. |
| 24 | Single page at site root: index.html, Single Family default on, drop offline dashboard | done | PR session/2026-06-21-index-root. (1) **Single Family category now starts checked** like every other (removed `DEFAULT_HIDE`; `initCats` selects all; URL omits `cats` unless something is hidden) — default badge is now all ~490K permits. (2) `build.py` rewritten to **standard-library** Python (no pandas) that publishes `src/city_explorer.html` as **`dist/index.html`** (+ copies `_headers`), so Cloudflare (output dir `dist`) serves the explorer at `/` — fixes the `krevian.com/` 404. (3) **Dropped the separate offline dashboard** and the now-superseded `_site`/`build-site.sh` deploy from #23: removed `src/template.html`, `dist/permit_dashboard.html`, the duplicate `dist/city_explorer.html`, `test/harness.js`, `test/drift.js`, `deploy/build-site.sh`, `requirements.txt`; `npm test` → just `city_harness.js` (65 green, SF-default assertions flipped); CI drops the pandas install. The dashboard exists ONLY as `index.html`. **CF settings:** build `python build.py`, output `dist`, preset None. README rewritten. (`data/raw/` now unused — left in place.) |
| 23 | Cloudflare Pages deployment | done | PR session/2026-06-21-permit-category-filter. Replaced GitHub Pages with **Cloudflare Pages** (Git-integration). _(superseded by #24: the `_site`/`build-site.sh` assemble was replaced by `build.py` → `dist/index.html`, output dir `dist`.)_ Added `deploy/build-site.sh` — a pure-copy assemble → `_site/` (explorer as `index.html` + offline dashboard + `_headers`); no Python/secrets needed in CF's build since the explorer is static and `dist/permit_dashboard.html` is committed. New root `_headers` adds security headers + a CSP scoped to the app's origins (jsdelivr/unpkg scripts, Google Fonts, `*.basemaps.cartocdn.com` tiles, `data.calgary.ca` fetch); CSP validated in-browser (Chart/Leaflet/live data/15-of-15 tiles/Inter all load, 0 violations). Removed `.github/workflows/pages.yml`; README hosting section rewritten. **CF dashboard:** build command `bash deploy/build-site.sh`, output dir `_site`, preset None. **User follow-up:** create/connect the CF Pages project (+ optional custom domain), then disable GitHub Pages in repo Settings once CF is confirmed. |
| 22 | Map: drop "Bubbles", add avg-permits/year shading | done | PR session/2026-06-21-permit-category-filter. City map is now ALWAYS a community choropleth (removed the Bubbles/Areas toggle, `drawBubbles`, `this.choro`, the `map=areas` URL param). Added **"Avg permits / year"** (`ppy` = community count ÷ selected year-span, log-scaled) as the new DEFAULT metric — it carries the volume signal the count bubbles used to. Boundaries preloaded at init so the default view isn't blank; boundary-load failure is now recoverable (in-map Retry link, no blank map). City harness 60→65 (asserts `ppy = n/yspan`, exercises the no-vals fallback + boundary-failure→retry cycle); removed dead `.lg-dot/.lg-sm/.lg-lg` CSS. Live-verified; src/dist synced; 16+65+9 green. 4-dimension review (31 raw items, ~20 were "no-fix" affirmations) → 5 real fixes applied. |
| 21 | Permit-category show/hide filter (city explorer) | done | PR session/2026-06-21-permit-category-filter. `src/city_explorer.html`: multi-select checkbox panel built dynamically from the distinct `permitclassgroup` values, **replacing** the single-select "Permit class" dropdown. All categories checked except **Single Family** (≈55% of permits; clutters the map) which starts unchecked. Server-side filter (`permitclassgroup IN (...)` in `where()`, omitted when all checked) so the count query — and the city/detail ~30K threshold — sees the FILTERED count; hiding Single Family can flip into detail view. Toggling re-runs the abort/`_seq`-guarded `apply()`; all-cleared → empty-state (no fetch); Select all / Clear all + "N of M shown"; shareable `?cats=` (encodes the HIDDEN set, default omitted, `cats=*`=show all). Single-`<script>` contract preserved; `esc()` untouched (drift green). City harness 40→59 incl. a category-aware count stub + city→detail flip assertion guarding the filtered-count invariant; src/dist synced; live-verified; 16+59+9 green. 4-dimension adversarial review → 3 findings, all fixed. |
| 26 | SEO: serve sitemap.xml + robots.txt from the deploy root | done | `build.py` published only `index.html` + `_headers` to `dist/`, so the `sitemap.xml` added at the repo root (commit fcd4960) never reached the Cloudflare output and 404'd at `krevian.com/sitemap.xml`. Generalized the copy step to a `PASSTHROUGH` list (`_headers`, `sitemap.xml`, `robots.txt`) so each root file is published to `dist/`. Added `robots.txt` (allow-all + `Sitemap:` → canonical `https://krevian.com/sitemap.xml`). Tidied `sitemap.xml` (dropped the xml-sitemaps.com external stylesheet, generator comment, and unused namespaces). Rebuilt dist (4 files); 72/72 city harness green. Follow-up (user, account-level): submit the sitemap in Google Search Console and confirm GitHub Pages is disabled now CF/krevian.com is canonical. |
| 28 | Move hosting to yyc-permits.krevian.com | done | Repo refs (README, sitemap.xml, robots.txt, dist/) updated to the subdomain (PR #16); custom domain connected and serving. `rel=canonical` + matching robots/sitemap shipped in PR #20. **Redirects (2026-08-23):** a Cloudflare Bulk Redirect 301s all four legacy hosts — `krevian.com`, `www.krevian.com`, `krevian.ca`, `www.krevian.ca` — to `https://yyc-permits.krevian.com/`, preserving path + query string; `krevian.ca` needed placeholder proxied A records (`@`/`www` → 192.0.2.1) first since its zone had none. Verified live: all four return 301 → subdomain, subdomain returns 200 (no loop). **Search Console (2026-08-23):** krevian.com + krevian.ca already verified as properties; sitemap `https://yyc-permits.krevian.com/sitemap.xml` submitted. Google will migrate the old URLs to "Page with redirect" over the coming weeks — no further action. Task complete. |
| 29 | Compact filter toolbar: categories → dropdown | done | PR #17. The sticky toolbar covered most of a laptop viewport (5 selects + search + 3-row category-checkbox grid + help line). Category grid now lives in a native `<details class="catdd">` dropdown in the filter row — "Categories N of M" button opens an overlay panel (absolute vs `.toolbar-row`, `z-index:70`, `max-height:min(60vh,480px)` internal scroll) with the chips + Select all/Clear all; panel overlays content, never pushes it. Removed the redundant `.toolbar-help` line (duplicated in "How this works"). Guarded (`if(document.addEventListener)`) outside-click/Esc close keeps the harness's stub document safe; panel stays open across checkbox toggles (renderCats only rewrites `#cat-list`). All harness ids + D method signatures unchanged; single-`<script>` contract intact; CI 72/72 green; dist rebuilt. |
| 30 | Repo audit (2026-07) — findings-first | done | Branch audit/permit-explorer-2026-07; report docs/permit-explorer-audit-2026-07.md (PR #19, merged 2026-08-23). 0 critical, 4 major, 11 minor across correctness/data-metric integrity, security & provenance, maintainability, conventions, test/CI coverage, and live deployment/responsive/a11y/SEO behavior. pi_audit + provenance clean; src/dist byte-identical; 83-assertion harness and 6-check live smoke green at audit time. Major #4 (a local working-tree edit that had deleted src/m0_validate.py) was resolved before the fix PR — committed validator intact. |
| 31 | Act on 2026-07 audit findings | done | PR #20 (session/2026-08-23-audit-fixes), 16 commits, every remaining major+minor actioned. **Majors:** avg-cost KPI/choropleth/compare now use `avg(estprojectcost)`/`count(estprojectcost)` instead of sum/count(1) (live 2026-08-23: $251,696 shown vs $272,724 true, −7.71%; 92.3% cost coverage now disclosed under the Total-cost KPI); compare-panel request failures become an `{error}` sentinel — excluded from table/chart/best-rankings, named in a `#cmp-notice` with Retry (`cmpRetry`), never zero-valued communities; mobile overflow fixed via `minmax(0,1fr)` + `.grid .card{min-width:0}` (static source contract in the harness; no browser dependency by choice). **Minors:** `rel=canonical`; speed-chart hint/aria/tooltip say median (detail) vs avg (city); negative days-to-issue excluded everywhere via `avg(case(diff>=0,diff))` (verified bit-identical to a filtered control on the live API; detail rows → null, About documents the rule); completion resolved = `count(statuscurrent)`−open in all three call sites; dedicated `newn` aggregate for % new construction; bubbles copy → choropleth wording; detail-CSV formula neutralization (leading `= + - @ TAB CR`; city-mode server CSV is a disclosed residual); SRI sha384 + `crossorigin` on all three CDN assets with chart.js pinned to `/dist/chart.umd.min.js`; ci.yml `permissions: contents: read`, all five action refs SHA-pinned, dependabot.yml (github-actions weekly); post-build dist-drift CI gate; harness extraction guard (smoke.js idiom). Harness 83→115 checks; smoke check #4 now runs the new head query verbatim (6/6 live). |
| 32 | Repo audit (2026-08) — security & quality | done | Branch audit/permit-explorer-2026-08; report docs/permit-explorer-audit-2026-08.md (PR #24, merged 2026-08-23). Full 47-commit history scanned. **No secret/API key exposed anywhere** — the only token (`SOCRATA_APP_TOKEN` in src/m0_validate.py) is env-sourced, never committed. 0 critical, 2 major, 9 minor. pi_audit 13 hits all false positives ("silent/silently" in reports/comments); git fsck + provenance clean. PR #20 delta independently re-reviewed and confirmed (SRI hashes match live CDN, SoQL null-handling consistent, harness genuinely adversarial). |
| 33 | Act on 2026-08 audit findings | done | **Majors (PR #25):** reflected XSS via `?cmp=` URL param (pre-existing since #15) — `esc()` didn't escape `"`, so a crafted share link injected an onclick event handler; fixed two ways — readURL validates names against the community list, and chip buttons use an index-based handler (`cmpRemoveIdx`) so no name is interpolated into an attribute (drill link also hardened). Stale public GitHub Pages build (frozen June/pre-#20 version serving the $0-imputed averages) — **GitHub Pages disabled**, URL now 404s. **Minors (PR #26):** LICENSE trimmed to verbatim MIT so GitHub detects it (attribution stays in README + footer); `cmpRetry` no longer clears the global error banner; choropleth avg-cost min-sample gate now counts cost-bearing permits (`costn`) not all permits; sitemap `lastmod` → 2026-08-23; `#cmp-notice` Retry moved out of the `role="status"` live region + status set via textContent; smoke.js guards `d` with `!= null`; removed the ~950 KB unused data/raw CSV. **GitHub-settings minors (user, 2026-08-23):** enabled secret scanning + push protection + Dependabot alerts/security updates, added `main` branch protection (require CI, block force-push), fixed the repo description typo + set homepage/topics. Harness 115→119 (adds the XSS-rejection + index-handler + a11y assertions); live smoke 6/6; dist byte-identical. |
| 34 | Development permits mode (dataset toggle) | done | **Shipped 2026-08-29** (PR #31; scoped 2026-08-28 in docs/dev-permits-scope.md). Development Permits (6933-unw5; 192,471 rows 1979→now) behind a Building·Development toolbar toggle (URL `ds=dp`) over a DATASETS config registry that absorbed every hardcoded endpoint/field/SoQL/column/metric — Phase-1 refactor proven zero-change (119 checks green + byte-identical SoQL-URL replay). DP mode: approval rate ≈97% from `decision` (A÷(A+R+DR)), days-to-decision (~51 d, case(≥0) guard, re-binned 0–7…180+ distribution), permitted-vs-discretionary, top applicants, Decision + Land-use table/CSV columns, DP status pills, choropleth ppy/days-to-decision/approval-rate (gated on decided count), SDAB-hearing insight; 29% null category stays visible as "(uncategorized)" via `(category IN (…) OR category IS NULL)`, re-verified to the row (67,905 = 11,276 + 56,629). Toggle keeps only deliberate year/community selections (a bound at the old dataset's default follows the new dataset's default — no invisible year filter), resets dataset-scoped filters/sort/search, keeps valid compare selections, caches per-dataset option lists (toggle-back refetches nothing), rebuilds charts (no cost line/units axis on DP). Harness 119→172 (fetch stub routed by resource id; canned DP branch set keeps the disagree-with-derivables property); smoke parameterized over the registry (api→fields→head extraction), 12/12 live on both endpoints; headless live driver verified totals/KPIs/IS-NULL identity/BELTLINE drill (4,002)/URL round-trips/cached toggle-back. Title→"Calgary Permits Explorer"; meta/hero/About/footer cover both datasets incl. the 1979–2000 legacy caveat. Phase-4 DP→BP address join split to task #35. |
| 35 | Application→construction pipeline (DP→BP address join) | done | **Shipped 2026-08-29** (PR #33, same day as the scope). "Application → construction" card in DP mode, overlay-locked until a community is drilled: released DPs joined to BP applications at the same parcel via parcelKey (uppercase/whitespace/strip `#unit `) over address ∪ locationaddresses; headline = followed within 3 y (+ ever share), median application→construction lag, share filed before the decision, n of eligible, per-category bars (min-n 20). Population restricted to DPs applied ≥ the BP data floor (derived from the cached bp year list; min(applieddate) probe on ?ds=dp deep links). One cached 2-field cross-dataset fetch per community (soql() gained an api arg; ≤30K cap disclosed); filters/search recompute client-side, seq-guarded. Stretch shipped: "Decision outcomes by year" stacked card (appr/refn ride cityYr in city, rows in detail — zero extra requests; DP grid row back to 3 cards). Harness 172→192 (crafted BP parcel history makes every stat hand-countable); smoke 12/12 (dp fields +locationaddresses); live-verified: Beltline 82% ever = the scoped 82.0%, Altadore 29% pre-decision, new-dwelling bars 94/97%, cache confirmed. **Scoped earlier the same day** (PR #32) — measured join rates (92.3% parcel match over 8,054 released DPs; category-split conversion 9–98%) and the 1999-floor correction, in docs/dp-bp-pipeline-scope.md. Multipoint rendering: dropped. |
| 36 | Make the npm build command work without a `python` alias | done | **Fixed 2026-08-30.** `npm run build` now invokes `python3 build.py`, matching the interpreter available on the development machine. README command copy synchronized; fresh build produced no `dist/` drift; offline harness remains 192/192 green. |
| 37 | Kitimat edition (BC) — companion page at `/kitimat/` | done | **Shipped 2026-09-02** (PR #36 squash-merged; live at https://yyc-permits.krevian.com/kitimat/, sitemap updated). Scoped the same day in docs/kitimat-scope.md. `fetch_kitimat.py` (stdlib) snapshots three sources into `data/kitimat/` — Cloudpermit public records (69 issued building permits Dec 2025→; permit no., address, category, opened/issued dates, point, PID; no cost/contractor/units/status; no CORS), BC Stats BPER (9 monthly series for SGC 5949005, Jan 2018→Jun 2026, `x`-suppressed cells kept as gaps; OGL-BC) and map.kitimat.ca ArcGIS (8 neighbourhood polygons simplified 278 KB→19 KB; OCP zone by PID join, 57/69 matched); point-in-polygon puts 27/69 permits on industrial lands outside every neighbourhood. `src/kitimat_explorer.html` = two panes (BC Stats trend 2018→; permits map/charts/table with neighbourhood·year·category·search filters, URL state, CSV) built by `build.py` into `dist/kitimat/index.html` with the snapshot embedded in JSON script blocks. Tests: `test/kitimat_harness.js` (71 checks, offline) + `test/test_fetch_kitimat.py` (16); `fetch_kitimat.py --check` = source-contract smoke. `.github/workflows/kitimat-refresh.yml` refreshes weekly and opens a PR only when the data changed (needs the "Allow GitHub Actions to create PRs" repo setting). Open: Phase 0 — ask the District (250-632-8900 / administration@kitimat.ca) for a historical permit export; separate subdomain vs `/kitimat/` path. |
| 38 | Map basemap: CARTO tiles now watermarked "API KEY REQUIRED" | todo | Found 2026-09-02 while rendering the Kitimat page: `basemaps.cartocdn.com` light/dark tiles carry an "API KEY REQUIRED — carto.com/basemaps/apikey" watermark for every request without a key (identical bytes with no Referer, localhost, and the production referer), so the **live Calgary map is affected too**. Options: (a) register a free CARTO API key and append `?api_key=` to the tile URL (key is public in the page; restrict it by referrer at CARTO), (b) switch to `tile.openstreetmap.org` (needs `img-src` in `_headers`; no dark variant — CSS `filter` for dark mode; OSM usage policy is fine for this traffic), (c) a keyed provider (Stadia/MapTiler). Same tile helper in both pages (`tileUrl()`), so one change each. |
| 39 | Kitimat page: global year-range filter driving both panes | done | **Shipped 2026-09-02** (requested after #37 went live: "filters that update all visuals — year filter is priority"). The toolbar moved above both sections and is sticky; a From/To year range (2018→newest permit year; reversed picks snap; axis ends = no URL params, else `from=`/`to=`, legacy `year=` honoured) recomputes the BC Stats KPIs/labels/charts (`bperSeries()` filters every series by year) and filters permits by issue year; section titles and a scope badge state the range; an insight explains empty permit panes for pre-Dec-2025 ranges. Harness 71→84 checks (single year, multi-year, snapping, reset, URL). |
| 40 | Kitimat: council planning-decisions dataset (DVP / DP / TUP / zoning / OCP) | todo | Scoped 2026-09-02 from the 2019–2022 minutes (docs/research/kitimat-council-minutes-2019-2022/): 82 of 87 regular-meeting minutes recovered via the Wayback Machine (old site PDFs now 404); first-pass extraction = 64 applications + 23 bylaws with address, outcome, resolution no. and first→last council date (median 42 d for the 14 seen at >1 meeting). Only source anywhere for variances, rezonings and temporary use permits; no building-permit issuance data in minutes. To ship: (1) hand-verify the 25 applications without a detected final vote, (2) commit an extraction script beside `fetch_kitimat.py`, (3) extend to 2023–2024 (same archived page) and 2025–2026 (`kitimat-docs.simplicitycms.ca/docs/minutes-<mon>-<d>-<yyyy>.pdf`), (4) geocode addresses via `Addresses/MapServer/1` and add a third pane under the global year filter. ~2 sessions. |
| 27 | Compare communities (side-by-side) | done | `src/city_explorer.html`: new city-view "Compare communities" card — pick 2–4 from a dropdown (colour-coded chips) and see a side-by-side metrics table (permits, total/avg cost, avg days-to-issue, completion rate, housing units, avg permits/yr; best-in-row highlighted) plus a permits-by-year line chart overlaying them. Each community is fetched independently (one race-guarded yearly group-by query each via `cmpRun`/`cmpWhere`, scoped to the current year/category/work/status filters but NOT the main community dropdown); totals/avg-cost/dti/completion are derived from those rows. City-mode only (hidden in detail); re-fetches when filters change; shareable via `?cmp=` (omitted when empty). Single-`<script>` contract preserved; city harness +11 (per-community `done`/`openn` yearly stub + data/totals/completion/chart/table/remove/clear/URL assertions); `dist/index.html` rebuilt byte-identical. |

## Log

- 2026-09-02 — Examined the 2019–2022 council minutes (task #40 scoped). Found the old Archive of Minutes page in the Wayback Machine, pulled 82/87 regular-meeting minutes (418 pages) and regex-extracted every planning decision: 64 DVP/DP/TUP applications and 23 bylaws with address, outcome and resolution number; 2020 is the peak year (11 DVPs, 8 TUPs, 9 zoning bylaws — the LNG build). No building-permit statistics exist in minutes; annual reports 2020–2024 on docs.kitimat.ca carry narrative counts only (new dwelling permits 2019: 15, 2020: 6, 2021: 59, 2022: 8). Meeting pages link Vimeo recordings with chapter timestamps, no transcripts. CSVs + notes committed under docs/research/. Session wrap-up: PR #36 merged + live (task #37 done), PR #38 (year filter, task #39) open with CI green; PR #37 (tracker-only) folded into #38 and closed.
- 2026-09-02 — Added the global year-range filter to the Kitimat page (task #39; branch session/2026-09-02-kitimat-year-filter). One toolbar above both panes; the range drives BC Stats and permits alike; verified in headless Chromium (2019–2021 → $549.5M / 0 permits with the explanatory insight; 2026 → 65 permits / $25.2M; `?from=2020&to=2023&nbh=Whitesail` restores). Also surveyed council agendas/minutes/video as a further data source (findings reported in-session; no code): agendas on docs.kitimat.ca (`YYYY-MM-DD-reg.pdf`, 2026 naming) and minutes on kitimat-docs.simplicitycms.ca (`minutes-<mon>-<d>-<yyyy>.pdf`); planning decisions (DVP/DP/TUP/zoning) are well-structured motions but only ~10–15 per year (2023 annual report: 6 DVP, 4 DP, 1 TUP, 1 zoning, 2 OCP); annual reports 2020–2024 on docs.kitimat.ca carry narrative counts (new dwelling permits 2019: 15, 2020: 6, 2021: 59, 2022: 8); meeting pages link Vimeo recordings with chapter timestamps, no transcripts.
- 2026-09-02 — Built the Kitimat edition (task #37) on session/2026-09-02-kitimat-edition following the scope: Phase 1 `fetch_kitimat.py` + committed snapshot (`data/kitimat/snapshot.json` 70 KB, `meta.json`) with parser/geometry unit tests and a `--check` contract smoke; Phase 2 `src/kitimat_explorer.html` (design system lifted from the Calgary page; two panes; harness-safe DOM usage) embedded by `build.py` into `dist/kitimat/index.html`; Phase 3 weekly refresh workflow that opens a data PR only on change, `npm test` now runs both harnesses plus the Python tests, sitemap/robots updated. Verified in headless Chromium: 69 permits, all charts drawn, map polygons + markers, dark mode, no horizontal overflow at 390 px. Found and logged task #38 (CARTO tile watermark, affects production).
- 2026-09-02 — Scoped a Kitimat, BC edition (task #37; planning only, no app code). Confirmed the District of Kitimat is the permit authority (Municipal Code Part 13; Cloudpermit since ~Jan 2026). Searched the BC Data Catalogue: the only usable dataset is BC Stats BPER (monthly city-wide value/units, 2018→; heavy suppression); BC Assessment permits are access-restricted; no municipal permit-level data exists provincially. Drove the Cloudpermit public-records SPA headless to find its `search-applications` endpoint (plain JSON, no auth, no CORS): 69 issued permits since Dec 2025 with five public attributes. Found the District's public ArcGIS Server (neighbourhoods, parcels with PID/OCP zone, addresses) and joined the 69 points to the 8 neighbourhoods (27 fall on industrial lands outside all of them). Conclusion in docs/kitimat-scope.md: a separate companion page over a weekly snapshot, not a DATASETS toggle; the real unlock is asking the District for its historical permit register.
- 2026-08-30 — Session wrap-up (task #36). Fixed the local build entrypoint after `npm run build` failed on a machine with Python 3 but no `python` alias: package.json now calls `python3 build.py`, and the README example matches. Verified the advertised npm command end-to-end; it rebuilt all four deploy artifacts with no committed `dist/` drift. The deterministic harness remains 192/192 green. No application behavior or production artifact changed.
- 2026-08-29 — Session wrap-up. All three of the day's PRs squash-merged to main — #31 (dataset toggle, task #34), #32 (pipeline scope, task #35) and #33 (pipeline + decision-outcomes cards, task #35 shipped) — and production at yyc-permits.krevian.com verified serving each deploy in turn (the Building·Development toggle and 6933-unw5 attribution, then card-pipeline/card-outcomes and the parcel-join code; `/?ds=dp` returns 200). Repo housekeeping: pruned all ten stale merged remote branches (the 2026-08-23 audit/fix/docs set, PRs #24–#29, plus the four #30–#33 session branches) — origin now carries `main` only; local tree clean, no stashes, no open PRs. Tracker state at close: every task through #35 is done or dropped, nothing in flight; the daily smoke cron now guards both Socrata endpoints (12 checks) and the offline harness stands at 192. Parked ideas if wanted later: multipoint rendering for `locationcount`>1 permits (dropped in the #35 scope) and a unit-level-first join tiebreak for downtown towers (noted there as later polish).
- 2026-08-29 — Shipped the application→construction pipeline (task #35; PR #33; branch session/2026-08-29-dp-bp-pipeline), implementing the same-day scope. The "Application → construction" card lives in DP mode using the renovation card's overlay idiom in reverse — locked until a community is drilled, since follow-ups need that community's full building-permit history. Join: `parcelKey()` (uppercase, whitespace-collapse, strip a leading `#unit `) over each released DP's `address` ∪ ';'-split `locationaddresses`, against a parcel→sorted-ISO-dates index of one cached cross-dataset fetch (`soql()` gained an optional `api` argument; `originaladdress,applieddate` only, ≤30K cap disclosed in-card if hit, ~14.6K worst case today). First BP application ≥ the DP application wins; stats — followed-within-3-years headline + ever share, median application→construction lag, share filed before the DP decision, n of eligible, per-category bars (min-n 20, top 6) — recompute client-side over the CURRENT filtered rows on every filter/search change with zero further requests, seq-guarded against community/dataset switches mid-flight. Population = released DPs applied ≥ the BP data's first year, derived from the cached bp year list (a `min(applieddate)` probe covers `?ds=dp` deep links), implementing the scope's 1999-floor correction. Stretch shipped too: "Decision outcomes by year" stacked Approval/Refusal card — per-year `appr`/`refn` ride the existing `cityYr` select in city mode and the loaded rows in detail, zero extra requests, and DP mode's distribution grid row is back to three cards (outcomes · days-to-decision · pipeline). dp detail fields gained `locationaddresses` (+ a `decided` date on mapped rows); About documents the metric and its same-parcel/parcel-granularity caveats. Harness 172→192: crafted BP rows for two generated-parcel cohorts make every stat hand-countable — a unit-prefixed BP predating all applications proves parcel-matching without follow-up credit, a far-future one splits ever from within-3y, one lands between application and decision (38% pre-decision), plus synthetic-row population tests (pre-1999 and non-released excluded), plKeys collapse, one-fetch-per-community caching, outcomes values in both modes, and card lock/visibility states across datasets. Smoke 12/12 (the dp field list now carries locationaddresses). Live-verified end-to-end: Beltline 82% ever — exactly the scoped 82.0% — 65% within 3 y, median 212 d; Altadore 29% filed-before-decision with new-dwelling category bars at 94%/97%; re-drill served from cache. dist rebuilt per commit.
- 2026-08-29 — Scoped the DP→BP pipeline analysis (task #35; planning only, no app code — the #17/#34 precedent; docs/dp-bp-pipeline-scope.md). Measured the address join live across five deliberately different communities (Beltline, Altadore, Evergreen, Saddle Ridge, Greenview Industrial; 8,054 released DPs): DP `address` and BP `originaladdress` share an identical format (`1108 4 ST SW`, `#unit ` prefixes on ~10% of both, no `;`/`,`), and uppercase+whitespace normalization alone matches 90.5% — parcel-keying (strip the `#unit ` prefix) plus expanding DP keys with the ';'-joined `locationaddresses` lifts it to **92.3%**, so the join is viable without an alias map. The followed-by-a-BP spread (31–82% per community) is category mix, not join failure: contextual/new dwellings 97.7%/94.7%, change-of-use ~85%, signs ~71–78%, but existing-building relaxations 29.7%, secondary suites 19.1%, home occupations 13.4%, compliance follow-ups 9.4% — the card should surface that contrast, not average it away. One load-bearing correction surfaced: c2es-76ed only starts 1999-06-22, so pre-1999 DPs score spurious "followed" hits from later unrelated permits; the population must be restricted to DPs applied ≥ the BP first year (data-driven). On that restricted population (7,248): 62.3% followed any-time, **52.4% within 3 years** (the recommended headline — an unbounded window adds ~7 pp of increasingly dubious matches), median lag 127 d measured application→application (release-based lag is muddled: 18.2% of follow-ups were applied while the DP was still undecided; Altadore's release-lag median is −1 d). Recommended shape: an "Application → construction" card in DP mode using the renovation card's locked-overlay idiom (live only when community-drilled, since follow-ups need the community's full BP history), one cached 2-field cross-dataset fetch (`soql()` gains an optional api argument; ≤30K cap with 2× headroom), client-side recompute over the filtered rows, per-category HTML bars (no new canvas). Harness plan: crafted rows for prefix-stripping/multi-parcel/BP-before-DP/no-match/concurrency, card-state and cache assertions. Decision-outcomes-by-year kept as a zero-request stretch; multipoint rendering dropped. Task #35 stays todo for the implementation session.
- 2026-08-29 — Shipped the development-permits mode (task #34; PR #31; branch session/2026-08-29-dev-permits), executing the 2026-08-28 scope in its three planned phases. **Phase 1 — config extraction, zero behavior change:** every hardcoded endpoint, field name, SoQL string, column list and per-dataset rendering rule (KPI renderer, insight templates, compare metrics, choropleth metrics, row mappers, CSV naming) moved into a `DATASETS` registry with a `D.ds` pointer; `soql()` reads `D.ds.api`; `where()`/`cmpWhere()` share a new `catClause()` helper that already carries the IS NULL branch; `init()`'s five option queries became a cached `loadOptions()` that rebuilds every dropdown and preserves selections where valid. Proven zero-change two ways: all 119 harness checks pass unchanged, and an old-vs-new replay of a full UI scenario (init, filtered wheres, drill, compare, sort) issued byte-identical SoQL URLs except the city-table `$select` field order (same 10 fields, registry order). **Phase 2 — DP mode:** segmented Building·Development control heading the toolbar; `DATASETS.dp` implements the scoped surface set — KPI six-pack (permits · discretionary share · released share · refusals · days-to-decision · approval rate = A÷(A+R+DR) from `decision`), bars-only volume chart, category doughnut, permitted-vs-discretionary bar, top applicants, decision-speed trend, re-binned 0–7/…/180+ days-to-decision distribution, Decision + Land-use table/CSV columns, DP status pill colours, fixed-radius category-coloured detail points with decision/land-use/applicant popups, choropleth ppy/days-to-decision/approval-rate (approval gated on decided count via the costn precedent), per-dataset insights incl. SDAB hearings (`count(sdabhearingdate)`), "(uncategorized)" checkbox → `(category IN (…) OR category IS NULL)` re-verified to the row (67,905 = 11,276 + 56,629); URL `ds=dp` applied before the option lists load, `?metric=cost` falls back to ppy under dp; title/meta/hero/About/footer rewritten to cover both datasets with the 1979–2000 legacy caveat and 6933-unw5 attribution. Three implementation findings vs. the scope: category is 44 real values + the null group (the scope's "45" had counted null); `latitude`/`longitude` are TEXT columns on 6933-unw5, so DP queries drop the `avg(lat/lng)` aggregates (dead weight since the bubbles were removed in #22 — the choropleth joins by name); and the live driver caught bp's default 1999 year-start silently scoping dp after a toggle, so year bounds now carry over only when deliberately chosen — a bound at the old dataset's default follows the new dataset's default. **Phase 3 — tests/docs:** harness fetch stub routes on the resource id before the select/group routing with a canned DP branch set whose aggregates deliberately disagree with derivable ratios (appr+refn ≠ n, so an n-denominator approval rate fails), 119→172 checks incl. toggle/filter-reset/IS-NULL-count/cached-toggle-back/URL-`ds=` assertions; smoke.js extraction parameterized over the registry (api→fields→head triple per entry, a documented key-order contract) running all six checks per dataset sequentially, 12/12 live; README + sitemap lastmod; dist rebuilt with every src commit. Phase 4 (DP→BP address-join pipeline) logged as task #35 for separate scoping, per the scope's recommendation.
- 2026-08-28 — Scoped the development-permits feature (task #34; planning only, no app code — the task-#17 precedent). Verified the Development Permits dataset (6933-unw5) live: 192,471 rows 1979→present on the same Socrata host (no CSP change); every SoQL feature the app uses works there (date_extract_y/m, case() bins, avg/sum/count(field), date_diff_d — avg ≈50.9 days applied→decision vs ≈23.9 applied→issued for building permits); communityname matches the boundaries join (top: BELTLINE 4,002); latitude/community coverage 99.7%, decisiondate 90.3%; category has 45 real values plus 29% null — pre-2000 legacy rows have essentially none (2 of 40,605) — and the "(uncategorized)" filter pattern `(category IN (…) OR category IS NULL)` was verified to the row (67,905 = 11,276 + 56,629). Recommendation: a Building·Development dataset toggle over a DATASETS config registry; a merged/union view was rejected (Socrata can't aggregate across datasets, cost/units exist on only one side, and one project often holds both a DP and a BP → double counting), as was enrichment-only (hides city-wide DP questions). DP surfaces: approval rate computed from the `decision` field (Approval 168,676 / Refusal 5,376 / Deemed 69 → ≈96.9% of decided) replaces completion rate; days-to-decision replaces days-to-issue with re-binned buckets; permitted-vs-discretionary replaces work type; applicant (permit expediters dominate) replaces contractor; cost/units cards drop; choropleth shades ppy / days-to-decision / approval rate. Full field mapping, per-surface changes, config sketch, harness/smoke plan (fetch stub routed on resource id; smoke extraction parameterized over both endpoints), risks and phasing in docs/dev-permits-scope.md. The address-join DP→BP pipeline card is deliberately split out as a later, separately-scoped task.
- 2026-08-23 — Security & quality audit + fixes (tasks #32–33), same day as the 2026-07 fix session below. Ran the repo-audit skill over the full 47-commit history: **no secret or API key exposed anywhere** (the sole token, `SOCRATA_APP_TOKEN`, is read from the environment in src/m0_validate.py and never committed), pi_audit's 13 hits all false positives (the word "silent/silently"), provenance clean. Report at docs/permit-explorer-audit-2026-08.md (PR #24). 0 critical, 2 major, 9 minor. An independent adversarial re-review of the PR #20 delta confirmed those fixes hold (SRI hashes recomputed against the live CDN and matched; SoQL null-handling consistent; harness anti-tautological). **Major #1 — reflected XSS via `?cmp=`:** the compare-chip remove button interpolated community names into a double-quote-delimited inline onclick, and `esc()` doesn't escape `"`, so `?cmp=x" onpointerover="alert(document.domain)` injected an event handler firing on hover (pre-existing since the compare panel, #15; bounded impact — no auth/cookies/secrets — but real in-origin script execution under the `'unsafe-inline'` CSP). Fixed in PR #25 two ways: readURL now accepts only known community names, and chip buttons call an index-based `cmpRemoveIdx(i)` so no name reaches an attribute; the choropleth drill link was hardened too. **Major #2 — stale public GitHub Pages:** shepherd70.github.io/permit-explorer/ was still serving a frozen June build (the $0-imputed averages #20 fixed, no canonical), orphaned since its deploy workflow was deleted in 30a3ca8 — disabled GitHub Pages via the API (now 404s). **Minors (PR #26):** LICENSE trimmed to verbatim MIT (GitHub now shows the MIT badge; attribution retained in README + footer); `cmpRetry` no longer clears the global banner (was hiding unrelated main-query errors); the choropleth avg-cost min-sample gate now counts cost-bearing permits (new `costn` on the community query) rather than all permits, since `avgc` averages only those; sitemap `lastmod` bumped (deriving it in build.py was rejected — would break the deterministic dist-drift gate); `#cmp-notice` Retry moved to a sibling button outside the `role="status"` live region with the status text set via textContent; smoke.js head check guards `d` with `!= null`; removed the ~950 KB unused data/raw export. Harness 115→119; live smoke 6/6; every src commit rebuilt dist byte-identically. **Account-level minors done by the user the same day:** enabled secret scanning, push protection, Dependabot alerts + security updates; added `main` branch protection (require CI, block force-push); corrected the repo description typo and set the homepage + topics. Four PRs (#24 report, #25 XSS/Pages majors, #26 minors) merged; branch protection means all future changes go via PR. Remaining open work is unchanged and unrelated: task #28 (Cloudflare Bulk Redirect for the apex/www/.ca hosts + Google Search Console sitemap submission).
- 2026-08-23 — Audit-fix session (tasks #30–31). Tidied the two parked GitHub items first: merged PR #18 (the 2026-07-12 wrap-up log) and opened+merged PR #19 for the never-PR'd audit-report branch (matching the #10 precedent), then branched `session/2026-08-23-audit-fixes` off the updated main and actioned every remaining 2026-07 audit finding in 16 commits (PR #20) — see task #31 for the full inventory. Planning verified the risky SoQL up front against the live API (single-pair `case()` returns null-on-unmatched; `avg(case(diff>=0,diff))` bit-identical to a `$where` control; `count(field)` = non-null count) and caught that Cloudflare Pages `_redirects` **cannot** redirect across hostnames — so the user-approved apex 301 (a reversal of July's "apex parked, no redirect") ships as `rel=canonical` in-repo plus a documented dashboard Bulk Redirect (README "Hosting"). Decisions taken with the user: SRI hashes over self-hosting (repo stays dependency-free); no Playwright — the un-layoutable mobile-overflow fix is guarded by static source contracts in the harness; TASKS.md conflicts avoided by merging #18 before branching. Harness grew 83→115 (avgc/costn/cn/sn/newn stubs that deliberately disagree with the old formulas, name-keyed `__failCmp` compare-failure injection + recovery, 40 reversed-date detail rows proving dti-null-not-zero, csvCell hostile-cell units, static canonical/SRI/minmax contracts, guarded script extraction); smoke #4 mirrors the new head query verbatim (6/6 live). Every src commit rebuilt `dist/` byte-identically; new CI steps enforce that and least-privilege/SHA-pinned actions going forward. One infra snag: the first push was rejected by GitHub email-privacy protection — repo-local git identity switched to the `134653832+shepherd70@users.noreply.github.com` convention used by all prior commits and the branch re-authored before pushing. Account-level follow-ups live in task #28 (Bulk Redirect + GSC).
- 2026-07-12 — Compact filter toolbar shipped (PR #17; task #29). User reported the sticky toolbar froze over most of a laptop viewport. The 16-checkbox permit-category grid moved from an always-open `.catbar` block into a native `<details class="catdd">` dropdown sitting in the filter row between Status and Search: the summary button shows "Categories" + the live `#cat-count` ("N of M", " shown" suffix dropped) + a rotating chevron (reused the `.howto` disclosure pattern); the panel is `position:absolute` against `.toolbar-row` (row-anchored so a 560px panel can't overflow mid-width viewports), `z-index:70`, internal scroll past `min(60vh,480px)`, and contains the old head row (label + Select all/Clear all) and the verbatim `#cat-list`. Deleted the `.toolbar-help` line (content duplicated in "How this works"). Added a fully guarded outside-click/Esc close block after `D.init()` — `if(document.addEventListener)` makes it a no-op under the harness's stub document; inside-panel clicks match `closest('#catbar')` so toggling chips never closes it, and since `renderCats()` only rewrites `#cat-list.innerHTML` the open state survives every `apply()` refetch. Empty-state copy now points at "the Categories menu" (still matches the harness's `/categor/i`). No element ids or D method signatures changed; single-`<script>` contract intact; CI harness 72/72; dist rebuilt via build.py. Visual pass was done by the user (browser-extension connection unavailable for an automated pass).
- 2026-07-12 — Domain move to yyc-permits.krevian.com started (PR #16; task #28). Repo-side references updated (README live link, `sitemap.xml` `<loc>`, `robots.txt` comment + `Sitemap:` line; dist/ rebuilt). The apex `krevian.com` will be left parked (owner's choice — no redirect). **Remaining (account-level, owner's):** Cloudflare Pages custom-domain swap (add subdomain → verify → remove apex; subdomain still NXDOMAIN as of end of session, apex still serving) and Google Search Console property + sitemap re-submission for the subdomain. Note: session began on a stale local branch (`session/2026-06-24-compare-mode`, already merged as PR #15) whose accidental re-push briefly recreated the remote branch — deleted; the domain commit was cherry-picked onto a fresh branch off origin/main (the recurring stale-local-branch trap).
- 2026-06-24 — Added a "Compare communities" panel to the city-wide explorer (task #27; branch session/2026-06-24-compare-mode). A new city-view card lets you pick 2–4 communities and compare them side by side: a metrics table (permits, total est. cost, avg cost/permit, avg days-to-issue, completion rate, housing units, avg permits/year — best value per row highlighted) and a permits-by-year line chart overlaying the selected communities. Each community is fetched on its own via a race-guarded `cmpRun()` (one yearly group-by query per community through `cmpWhere()`, scoped to the current year/category/work/status filters but independent of the main community dropdown); totals/avg-cost/dti/completion are derived client-side from those yearly rows. The panel is city-mode only (hidden in detail), respects all active filters (re-fetches via `enterCity` when they change), and is shareable via `?cmp=<names>` (omitted when none). Kept the single-`<script>` harness contract and guarded the new Chart usage; the city harness gained a per-community `done`/`openn` yearly stub plus 11 assertions (per-community fetch, summed totals, completion = done/resolved, chart series count, table render, remove/clear, URL round-trip). `src/city_explorer.html` rebuilt to `dist/index.html` (byte-identical). Note: built on a fresh branch off current `main` after an earlier local session branch was found ~25 commits stale (its planned choropleth had already shipped as #17/#22). Validation is CI-only (no local Node); pushed for the harness to run.
- 2026-06-23 — SEO publish fix (PR session/2026-06-23-seo-robots-sitemap; task #26). The `sitemap.xml` committed to the repo root (fcd4960) was never actually served: `build.py` copied only `index.html` and `_headers` into `dist/`, and Cloudflare Pages serves `dist/`, so `krevian.com/sitemap.xml` 404'd. Generalized the copy step to a `PASSTHROUGH` list — `_headers`, `sitemap.xml`, `robots.txt` — each published to the output root when present. Added `robots.txt` (allow-all crawl + `Sitemap: https://krevian.com/sitemap.xml`) and tidied `sitemap.xml` to a minimal valid urlset (removed the generator's external CSS stylesheet ref, unused namespaces, and comment). `python build.py` now emits 4 files (index.html, _headers, sitemap.xml, robots.txt); `npm test` 72/72 green. Remaining steps are account-level (the user's): submit the sitemap in Google Search Console and disable GitHub Pages now Cloudflare/krevian.com is canonical.
- 2026-06-21 — Fixed two reported explorer issues (PR session/2026-06-21-index-root; task #25). (1) **Community filter is now a real dropdown** — a `<select>` listing all 317 communities A→Z with an "All communities" option (reset / show-all), replacing the free-text `<input>`+`<datalist>` that didn't read as a pull-down and required an exact typed match. (2) **Search is now instant** — it no longer issues Socrata's `$q` full-text query (the `count(1)+$q` aggregate measured ~4s): a new `D.onSearch()` filters the already-loaded detail rows **client-side** (instant, zero network; the count badge shows the filtered total), and in city view — where no rows are loaded — it shows a "narrow first" hint (`#search-hint`) rather than running the slow query. City harness 65→72 (client-side-filter, no-server-request, and city-hint assertions); live-verified by drilling into Harvest Hills and searching "160" → 32 rows with 0 server requests; `dist/index.html` rebuilt and synced.
- 2026-06-21 — Collapsed the project to a single page served at the site root (PR session/2026-06-21-index-root; task #24). (1) **Single Family category now defaults ON** — removed the `DEFAULT_HIDE` exception so every permit-category checkbox starts checked; the count badge now reflects all ~490K permits by default and the URL omits `cats` unless something is hidden. (2) **`build.py` now outputs `dist/index.html`** — rewritten to standard-library Python (dropped pandas) that copies `src/city_explorer.html` → `dist/index.html` and `_headers` → `dist/_headers`, asserting `index.html` exists; this makes Cloudflare (output dir `dist`) serve the explorer at `/`, fixing the `krevian.com/` 404 (the root had no `index.html`). (3) **Dropped the standalone offline dashboard** and the now-superseded `_site`/`build-site.sh` deploy from #23: removed `src/template.html`, `dist/permit_dashboard.html`, the duplicate `dist/city_explorer.html`, `test/harness.js`, `test/drift.js`, `deploy/build-site.sh`, and `requirements.txt`; `npm test` is just `test/city_harness.js` (65 green, with the Single-Family-default assertions flipped) and CI no longer installs pandas. The dashboard exists ONLY as `index.html`, never duplicated at a second URL. Verified the built `dist/index.html` in-browser: explorer loads, Single Family checked (16/16 shown), badge 490,787, clean URL. README rewritten for the single-page reality. (`data/raw/` is now unused — left in place; can be removed.)
- 2026-06-21 — Migrated hosting from GitHub Pages to **Cloudflare Pages** (Git-integration; task #23). Added `deploy/build-site.sh` — a pure-copy assemble step that builds `_site/` (the explorer as `index.html`, the committed offline dashboard, and a `_headers` file), so Cloudflare's build needs no Python or secrets (the explorer is static and `dist/permit_dashboard.html` is committed). The new root `_headers` adds security headers (nosniff, Referrer-Policy, X-Frame-Options, Permissions-Policy, HSTS) and a Content-Security-Policy scoped to the app's real origins — `'unsafe-inline'` scripts/styles (the app uses an inline `<script>` + inline handlers), jsdelivr/unpkg for Chart.js/Leaflet, Google Fonts, `*.basemaps.cartocdn.com` map tiles, and `data.calgary.ca` for the API/boundary fetches. Validated the CSP by enforcing it in-browser (injected as a `<meta>`): Chart.js, Leaflet, the live data (222,316 with Single Family hidden), boundaries, 15/15 map tiles, and the Inter font all load with **zero violations**. Removed `.github/workflows/pages.yml` (user chose to replace GH Pages) and rewrote the README hosting section. CF dashboard settings: build command `bash deploy/build-site.sh`, output dir `_site`, framework preset None. Remaining manual steps are the user's: create/connect the Cloudflare Pages project (+ optional custom domain) and disable GitHub Pages in repo Settings once CF is confirmed.
- 2026-06-21 — Flagged the upcoming deployment-target move from GitHub Pages to **Cloudflare Pages** (task #23). Verified the explorer is already portable to a domain root — no `/permit-explorer/` base-path hardcoding in `src/`, URL state derives from `location.pathname`, and the data API + boundary GeoJSON are absolute, CORS-enabled `data.calgary.ca` URLs — so no app code changes are required to move hosts. Remaining migration work is config-level: a CF Pages build (`python build.py` + an output dir whose `index.html` is the explorer, mirroring `pages.yml`'s assemble step) and optional `_headers` CSP/SRI hardening for the public deploy.
- 2026-06-21 — Reworked the city map: removed the "Bubbles" display option so it is now ALWAYS a community choropleth, and added "Avg permits / year" as a new shading metric and the default (PR session/2026-06-21-permit-category-filter). `ppy` = a community's permit count ÷ the selected year-range span (log-scaled) — the volume signal the proportional-count bubbles used to carry, so the default map still answers "which communities are building the most?". Removed `drawBubbles()`, `setChoro()`, the bubbles/areas segmented control + its CSS, the `this.choro` state, and the `map=areas` URL param (legacy `?map=areas` links are now ignored; the `metric=` param omits the new default `ppy`). Community boundaries are preloaded at init so the default city view isn't blank while the 2.1 MB GeoJSON loads. Because removing bubbles also removed the old boundary-failure fallback, the boundary-load error path is now recoverable and map-integrated (an in-map "Retry" link + the Community filter as an alternative, instead of a blank map). City harness 60→65: asserts `ppy = n/yspan` (incl. the `+1`), exercises the no-vals fallback (bare boundaries, not blank), and the boundary-failure→retry→recovery cycle; removed now-dead `.lg-dot/.lg-sm/.lg-lg` bubble-legend CSS and refreshed a stale comment. Live-verified in-browser (choropleth of 313 community polygons, DOWNTOWN = 14,597/28 = 521.3/yr, metric switching + URL round-trip, drill→detail, and the boundary-failure Retry all confirmed). `src`/`dist` synced; 16 + 65 + 9 green. A 4-dimension adversarial review returned 31 "confirmed" items, but ~20 were "code is correct" affirmations; the 5 genuinely-actionable ones (boundary-failure resilience, two harness coverage gaps, dead CSS, stale comment) were all fixed.
- 2026-06-21 — Added a permit-category show/hide filter to the city-wide explorer (`src/city_explorer.html`; PR session/2026-06-21-permit-category-filter). A multi-select checkbox panel — built dynamically from the distinct `permitclassgroup` values (Single Family, Commercial, Two Family, Garage, …) — replaces the old single-select "Permit class" dropdown. Every category starts checked except **Single Family** (≈55% of permits; clutters the map), which starts unchecked so the default view excludes it but leaves it one click away (if a data pull has no Single Family, everything starts checked). The filter is server-side (`permitclassgroup IN (<checked set>)` added to `where()`, omitted when all are checked, defensive `IN ('')` when none), so the count query — and therefore the city/detail ~30,000 threshold — evaluates the FILTERED count: hiding Single Family can flip a scope into detail view. Toggling re-runs the existing AbortController/`_seq`-guarded `apply()` (a refetch — city mode has no client-side rows to filter); clearing every box short-circuits to an empty state with no network call; a "Select all / Clear all" control and an "N of M shown" count round it out. View state is shareable via `?cats=` (encodes the HIDDEN set; default omitted for clean links, `cats=*` = show all; self-heals on load). Kept the single-`<script>` harness contract and guarded every browser-only call; `esc()` left untouched so `drift.js` stays green (the one new attribute sink escapes its own `"` inline). City harness extended 40→59 — including a category-aware count stub plus a city→detail flip assertion that guards the "threshold on the filtered count" invariant (verified it hard-fails if the `IN` clause is dropped from the count query). Verified live in-browser against the API (16 categories, Single Family off by default, badge 222,316 = 490,787 − 268,471; toggling, the flip to 490,787, and the all-cleared empty state all confirmed). `src/` and `dist/` synced byte-identical; 16 + 59 + 9 tests green. A 4-dimension adversarial review (correctness · harness/tests · security · UX/a11y) surfaced 3 confirmed findings — the untested flip invariant, a latent `aria-label` quote-escape gap, and a focus-ring polish — all fixed.
- 2026-06-21 — Fixed the two audit "major" findings and added a drift guard (PR session/2026-06-21-audit-fixes). (1) Offline dashboard XSS: `src/template.html` now HTML-escapes every untrusted permit field (table rows, Leaflet popup, insights) via the same `esc()` helper the live explorer uses — verified an `<img onerror>` payload is neutralized in all three sinks. (2) `build.py` cost parsing is now dtype-robust (`astype(str)` before `.str.replace`) so a future numeric/empty `EstProjectCost` export can't crash the build (verified on numeric + empty inputs). Also synced the drifted `fmt$`/`fmtN` helpers to the live copy and added `test/drift.js` (wired into `npm test`) that extracts each shared helper from `city_explorer.html` and fails if `template.html` diverges. Rebuilt dist; tests 16 + 40 + 9 all green.
- 2026-06-21 — Ran a findings-first repo audit (branch audit/permit-explorer-2026-06; report docs/permit-explorer-audit-2026-06.md; PR #10). Result: 0 critical, 2 major (both offline-path), 17 minor (mostly live/offline drift + routine hardening). pi_audit clean (5 false positives), git history/provenance clean. Majors + a drift-prevention guard actioned; remaining minors left for a follow-up sweep.
- 2026-06-12 — Dashboard built and verified; deep-dive analytics added; repo created.
- 2026-06-12 — City-wide live-API explorer added: aggregate city mode + permit-level detail mode, community bubble map with drill-down, community/class/work/status filters.
- 2026-06-12 — Cost & days-to-issue distributions now work city-wide via server-side case() binning; detail threshold raised 8K -> 30K (any single year city-wide now unlocks detail mode).
- 2026-06-12 — Session closed: harnesses committed under test/; PR opened session/2026-06-12-repo-setup → main.
- 2026-06-13 — Redesigned src/city_explorer.html into a public-facing tool: hero + example questions, "how it works" disclosure, restyled toolbar/KPIs/cards/table, Okabe-Ito colourblind-safe chart palette, CARTO light/dark basemap, map legend, intentional loading/empty/error states, dark-mode toggle, accessibility (skip link, focus states, keyboard-sortable headers, reduced-motion). Live API, all filters, city/detail logic, and city_harness.js all preserved. Synced to dist/.
- 2026-06-13 — "Harden & ship v1.0" pass (tasks #9–13): fixed the request-race in the live explorer (AbortController + generation tokens); added shareable URL filters and CSV export; CDN-failure guard, 429 message, empty-state pagination, map/paginator a11y; rewrote both harnesses as real assertions (incl. an overlapping-apply race test) with `npm test`; added GitHub Actions CI + Pages deploy; added MIT LICENSE with Calgary data attribution; README updated (hosting, testing, data/license sections, clarified scope). Two survey findings were verified as false positives and skipped: detail-mode never truncates at 30K (it's only entered when total ≤ 30K), and the year dropdowns are already data-driven.
- 2026-06-14 — v1.0 shipped: PR #3 (session/2026-06-13-harden-ship-v1) merged to main via merge commit (22568e8); session branch deleted and stale refs pruned. All build/hardening work (tasks #1–13) now on main. Added forward-looking tasks #14–16 (hosting/launch, live-API smoke test, offline data freshness) as the next priorities.
- 2026-06-14 — Evaluated converting the city-wide community map from proportional-symbol bubbles to a gradient choropleth (planning only, no code). Conclusion: keep bubbles for counts, add a choropleth toggle for an intensive metric; implement in Leaflet (`L.geoJSON`), not R/ggplot2. Logged as task #17 with the data-suitability/normalization, classification, colour, no-data, legend, and join/CRS plan and the key risks.
- 2026-06-17 — Merged PR #5 (perf: vectorized `build.py` permit cleaning — column-wise `clean_col` + `usecols` replacing the per-row `iterrows` loop; behavior preserved, CI green) into main (merge commit 7b144b5; branch deleted).
- 2026-06-17 — Launched (task #14): scanned the full git history for secrets (clean — no tokens/keys; only committed data is public Calgary open data), made the repo public, enabled GitHub Pages with the GitHub Actions source, and triggered the deploy. The city-wide explorer is now live at https://shepherd70.github.io/permit-explorer/, with the offline dashboard at /permit_dashboard.html. Pages redeploys on every push to main.
- 2026-06-17 — Bumped CI/Pages workflow actions to their Node 24 versions (checkout v4→v6, setup-python v5→v6, setup-node v4→v6, upload-pages-artifact v3→v5, deploy-pages v4→v5), clearing the Node 20 deprecation warnings from the deploy.
- 2026-06-20 — Closed task #18: the map view is now part of the shareable URL state. `writeURL` adds `map=areas` when the choropleth is active (omitted for the default bubbles) and `metric=<dti|comp>` for a non-default metric (the default `cost` is omitted to keep links tidy); `readURL` restores both and syncs the metric `<select>`. `setChoro`/`setChoroMetric` now call `writeURL`. A shared `?map=areas` link self-heals on first paint: `updateMap` lazy-loads boundaries via a new idempotent `ensureBoundaries()` and redraws as a choropleth, which let `setChoro` collapse to set-state + `writeURL` + `updateMap`. City harness extended with a `location`/`history` stub and 6 URL round-trip assertions (now 40/40); src/ and dist/ synced.
- 2026-06-20 — Closed task #17: added the community-map choropleth toggle to the city-wide explorer. In city mode the map switches between proportional bubbles (permit count) and a choropleth that shades each community by an intensive metric — avg project cost, avg days-to-issue, or completion rate — chosen from a selector; all three come from the existing community aggregate query (extended with `avg(date_diff_d(issueddate,applieddate))` and `case()` sums for completed/open status), so there are no extra requests. Boundaries are lazy-fetched once from Calgary's `surr-xmvs` GeoJSON (~2.1 MB, cached) and joined to permits on `UPPER(name)`↔`communityname` — a direct match covering ~99.4% of permit volume (270/317 communities; residual/N-A/CFB/new areas fall through to a distinct no-data fill, so no alias map was needed for v1). Classification uses a colour-blind-/greyscale-safe cividis ramp, a robust 2nd–98th-percentile clamp (log-scaled for the skewed cost metric) and a min-n gate, with a continuous gradient legend; the choropleth respects all active filters (it reads the filtered community query). Every new Leaflet `L.geoJSON` call is data-gated and guarded so the single-`<script>` eval contract is preserved; the city harness was extended with an `L.geoJSON` stub, a canned boundaries response (including an unmatched feature to exercise the no-data path) and 5 new assertions (now 34/34). `src/city_explorer.html` and `dist/city_explorer.html` kept byte-identical (LF). Known follow-up: the bubbles-vs-areas view state and chosen metric are not yet URL-encoded for sharing.
- 2026-06-20 — Closed task #16: made the offline dashboard's freshness metadata build-time derived instead of hardcoded. The footer previously read a literal "data embedded from Building_Permits_20260612.csv · generated 2026-06-12" and the subtitle a literal "1999–2026", both of which would silently go stale on the next rebuild with different data. Added `__COVERAGE__`/`__FOOTER__` placeholders to `src/template.html` (in markup, outside the single `<script>`, so the test-harness eval contract is preserved) and had `build.py` compute the coverage range, permit count, and a "data as of" export date — from the export's `YYYYMMDD` filename, falling back to file mtime — to fill them; the build now asserts all three placeholders exist. The footer also names the live explorer as the canonical always-current artifact, and the README frames the offline file as a point-in-time snapshot/demo (the tracker's suggested alternative). Rebuilt `dist/`; offline harness still 16/16.
- 2026-06-20 — Closed task #15: added a live-API smoke test (`test/smoke.js`, `npm run smoke`) that hits the real c2es-76ed Socrata endpoint and asserts the explorer's contract still holds — all 16 fields selectable (falls back to per-field probing to name a broken column), dataset count plausible (>100k), and the SoQL features used (`date_extract_y`, `sum`/`avg`/`date_diff_d`, `case()` binning, `.csv` export header) still parse. It self-extracts the endpoint + field list from `src/city_explorer.html` so it can't drift, retries transient 429/5xx/network errors but fails loudly on a real schema change. Wired to a daily scheduled workflow (`.github/workflows/smoke.yml`, + manual dispatch) that emails on failure; deliberately excluded from `npm test` to keep CI offline/deterministic. Verified live: all 6 checks pass, and a deliberately bogus column was confirmed to return HTTP 400 `no-such-column`.
